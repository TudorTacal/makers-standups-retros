import React from 'react';
import ReactDOM from 'react-dom';
import landingPage from '../views/landingPage.jsx';

ReactDOM.render(
  < landingPage title = 'Standups and Retros' />,
  document.getElementById('root')
);
